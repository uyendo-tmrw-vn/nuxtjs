{
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "node .output/server/index.mjs",
    "clean-nuxt": "rmdir /s /q .nuxt",
    "clean-nm": "rmdir /s /q node_modules",
    "clean-output": "rmdir /s /q .output",
    "clean-dist": "rmdir /s /q dist"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "6.1.3",
    "nuxt": "3.0.0",
    "nuxt-directus": "3.2.2"
  }
}
